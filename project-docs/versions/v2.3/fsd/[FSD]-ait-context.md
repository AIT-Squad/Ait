<!-- @id:[FSD]-ait-context -->
## context FSD

<!-- @summary: 聚焦上下文装配域。details 到对应 TDD（一文件一映射）。 -->

### 功能范围

ait context：L1 目标 chunk + L2 specgraph 依赖。

<!-- @id:[FSD]-ait-context:context_assembler -->
## context_assembler

<!-- @summary: ContextAssembler：assemble(L1 目标 chunk + L2 SpecGraph 依赖)，prd-to-impl / impl-edit details [TDD]-context_assembler。 -->

### 功能描述

ContextAssembler：assemble(L1 目标 chunk + L2 SpecGraph 依赖)，prd-to-impl / impl-edit 场景，focus/deps 选项。
