id: chg-103
version: v1.9
type: ADD
target: impl/skill-cli-doc-rewrite#impl-skill-cli-wrapper-verify
author: system
date: '2026-06-06T11:26:16.353085+00:00'
message: 'impl: inherit wrapper self locate verification'
base_hash: null
base_content: null
new_content: '<!-- @id:impl-skill-cli-wrapper-verify -->

  ## wrapper 自定位回归测试


  <!-- @ref:prd-skill-cli-resolution rel:implements -->


  ### 现状

  `skill/ait/bin/ait`（Unix）与 `skill/ait/bin/ait.cmd`（Windows）已通过 `$(dirname "$SCRIPT_PATH")/..` 与 `%~dp0` 实现脚本自定位，**本期不修改其自定位逻辑**——仅
  `impl-skill-cli-config-injection` 在 exec 前追加 `export AIT_SKILL_DIR`。


  ### 新增产出

  新增 `skill/ait/scripts/verify-wrapper-self-locate.sh`：

  - 在 5 个不同 cwd（`/tmp`、`$HOME`、`$SKILL_DIR`、`$SKILL_DIR/bin`、随机临时目录）下分别绝对路径调用 `$SKILL_DIR/bin/ait --version`。

  - 断言每次输出均为 `ait, version <X.Y.Z>` 且 exit 0。

  - 断言进程内 `AIT_SKILL_DIR` 环境变量等于 `$SKILL_DIR`（通过 `bin/ait` 增加一个隐藏诊断子命令 `__diag` 输出环境变量？或直接在 venv 用 `python -c` 读取，脚本里实现）。


  ### 集成入口

  - `skill/ait/scripts/verify-all.sh` 追加调用 `verify-wrapper-self-locate.sh`。

  - 失败则脚本以 exit 1 退出，CI/本地 verify 都能捕获回归。


  ### 边界

  - 不实现 Windows 版 `verify-wrapper-self-locate.cmd`（团队主开发环境为 macOS/Linux；Windows 由用户实际运行验证）。

  - 不引入 `pytest` 依赖（保持 verify scripts 轻量）。


  <!-- @ref:prd/v1-5-roadmap#prd-skill-cli-resolution rel:implements -->'
