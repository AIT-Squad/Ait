id: chg-179
version: v2.8
type: MODIFY
target: tdd/[TDD]-hash_utils#[TDD]-hash_utils
author: system
date: '2026-06-27T11:04:04.153930+00:00'
message: 'v2.8: generative foundation domain (root/io/yaml/hash TDD+FSD)'
base_hash: null
base_content: '<!-- @id:[TDD]-hash_utils -->

  ## hash_utils TDD


  ```yaml

  target_file: skill/ait/ait/hash_utils.py

  ```


  ### 技术栈


  Python 3.10+ / pydantic / PyYAML / click（按需）。


  ### 文件职责


  chunk_hash：chunk 内容哈希（用于 base_hash 失效检测）。


  ### 单元测试要求


  测试位于 tests/（hash 相关）：覆盖正常路径、边界与错误路径。'
new_content: '<!-- @id:[TDD]-hash_utils -->

  ## hash_utils TDD


  ```yaml

  target_file: skill/ait/ait/hash_utils.py

  ```


  ### 技术栈

  Python 3.10+；`hashlib`。


  ### 代码结构与契约

  - `normalize(text) -> str`：`replace("\r\n","\n").replace("\r","\n").strip()`（统一换行 + 去首尾空白）。

  - `chunk_hash(content) -> str`：`hashlib.sha256(normalize(content).encode("utf-8")).hexdigest()[:8]`（8 位十六进制前缀）。

  - `file_hash(path_content) -> str`：同 `chunk_hash`（用于 .doc-sync/ 跟踪代码文件）。


  ### 关键约定

  哈希前必须 normalize（CRLF/CR→LF + strip），保证跨平台/空白差异不影响指纹；8 hex 前缀用于 chunk 失效检测的 base_hash。


  ### 单元测试要求

  `tests/`（hash 相关）：normalize 幂等、相同规范化内容同哈希、CRLF/LF 等价。pytest。'
