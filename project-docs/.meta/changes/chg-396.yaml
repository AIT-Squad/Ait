id: chg-396
version: v2.31
type: MODIFY
target: fsd/[FSD]-ait-new_model#[FSD]-ait-new_model
author: system
date: '2026-07-13T11:59:31.205346+00:00'
message: 'docs carry zero relations: strip depends_on blocks from FSD bodies + PRD @ref to explicit edge; specgraph is sole
  store'
base_hash: null
base_content: '<!-- @id:[FSD]-ait-new_model -->

  ## new_model FSD


  ### 功能范围


  新模型 PRD/FSD/TDD 文档与代码生成上下文。创建文档、显式建关系边、图合法性与 target_file 唯一性校验、**六不变式约束（写时局部门禁＋confirm 全局门禁的校验内核）**、codegen 上溯组装。


  ### 交互契约


  - 创建：`create_prd/create_fsd/create_tdd`（写版本工作区、ensure 版本 meta；TDD 必含 target_file，**写入前按归一化路径做制品唯一属主门禁**）。**create_prd 是流转入口（阶段机起点
  phase→prd-creating；CLI 无活动版本时经 next_version_name 自动开版本）。**

  - **PRD 层流转**：`confirm_prd_layer`（冻结：锁 PRD chunk＋phase→prd-confirm）与 `revert_prd_layer`（成对返工：uncommit 解锁＋phase 回退）——门禁配返工，无终态陷阱。

  - **FSD 层流转**：`decompose_fsd(parent,child,content?)`（"拆分即建边"：父侧预检→原子写子 FSD→建 decomposes 边，取代 fsd link）＋ `confirm_fsd_layer/revert_fsd_layer`（冻结-返工对，phase
  fsd-creating/fsd-confirm）。

  - **TDD 层流转**：`create_tdd(...,parent_chunk_id)`（"创建即建 details 边"：父侧预检→写 TDD→建 details 边）＋ `confirm_tdd_layer/revert_tdd_layer`（冻结-返工对，phase
  tdd-creating/tdd-confirm）。**关系边只由 fsd decompose（decomposes）与 tdd create（details）在写内容时原子建立，link 命令退役**（幽灵边从入口消灭）。

  - 建边：三种关系全部随内容创建原子出生——**depends_on 随 `fsd create` 的 split 内 yaml 声明（`depends_on: [兄弟]`，同父简写解析；owned-scope 全量对账：文件＝兄弟依赖边的所有权边界，改/删声明即改/删边）**、decomposes
  随 `fsd decompose`、details 随 `tdd create --parent`；`add_edge(src,dst,rel)` 纯内部原语（CLI 无任何 link/depend 命令），**落盘前经 `check_edge_write`
  局部门禁——幽灵端点/TDD 第二父/PRD 第二 FSD 拒绝，零落盘可重试**。

  - 版本入口收口：fsd/tdd create 对不存在的版本报 `VERSION_NOT_FOUND`（幽灵版本 R3-04 完全体）；prd create 保留自动开版本（入口层语义）。

  - codegen：`prepare_codegen(version,tdd) -> CodegenBundle`（target_file + 上溯 FSD/PRD + 依赖契约；组合视图 chunk_id 世界，modify 中的 chunk
  上下文完整）。

  - 校验：`validate_prd_fsd_tdd_graph` + `validate_target_file_uniqueness`（归一化）+ **`validate_invariants`（六不变式：PRD↔1FSD、TDD↔1FSD/1制品、制品↔1TDD、关联经
  chunk、无孤儿、可追溯；另检树关系环——depends_on 环可诊断不拦）——供 version confirm 全局门禁消费**。'
new_content: '<!-- @id:[FSD]-ait-new_model -->

  ## new_model FSD


  ### 功能范围


  新模型 PRD/FSD/TDD 文档与代码生成上下文。创建文档、显式建关系边、图合法性与 target_file 唯一性校验、**六不变式约束（写时局部门禁＋confirm 全局门禁的校验内核）**、codegen 上溯组装。


  ### 交互契约


  - 创建：`create_prd/create_fsd/create_tdd`（写版本工作区、ensure 版本 meta；TDD 必含 target_file，**写入前按归一化路径做制品唯一属主门禁**）。**create_prd 是流转入口（阶段机起点
  phase→prd-creating；CLI 无活动版本时经 next_version_name 自动开版本）。**

  - **PRD 层流转**：`confirm_prd_layer`（冻结：锁 PRD chunk＋phase→prd-confirm）与 `revert_prd_layer`（成对返工：uncommit 解锁＋phase 回退）——门禁配返工，无终态陷阱。

  - **FSD 层流转**：`decompose_fsd(parent,child,content?)`（"拆分即建边"：父侧预检→原子写子 FSD→建 decomposes 边，取代 fsd link）＋ `confirm_fsd_layer/revert_fsd_layer`（冻结-返工对，phase
  fsd-creating/fsd-confirm）。

  - **TDD 层流转**：`create_tdd(...,parent_chunk_id)`（"创建即建 details 边"：父侧预检→写 TDD→建 details 边）＋ `confirm_tdd_layer/revert_tdd_layer`（冻结-返工对，phase
  tdd-creating/tdd-confirm）。**关系边只由 fsd decompose（decomposes）与 tdd create（details）在写内容时原子建立，link 命令退役**（幽灵边从入口消灭）。

  - 建边：三种关系全部随内容创建原子出生——**depends_on 随 `fsd create` 的 split 内 yaml 声明（`depends_on: [兄弟]`，同父简写解析；owned-scope 全量对账：文件＝兄弟依赖边的所有权边界，改/删声明即改/删边）**、decomposes
  随 `fsd decompose`、details 随 `tdd create --parent`；`add_edge(src,dst,rel)` 纯内部原语（CLI 无任何 link/depend 命令），**落盘前经 `check_edge_write`
  局部门禁——幽灵端点/TDD 第二父/PRD 第二 FSD 拒绝，零落盘可重试**。

  - 版本入口收口：fsd/tdd create 对不存在的版本报 `VERSION_NOT_FOUND`（幽灵版本 R3-04 完全体）；prd create 保留自动开版本（入口层语义）。

  - codegen：`prepare_codegen(version,tdd) -> CodegenBundle`（target_file + 上溯 FSD/PRD + 依赖契约；组合视图 chunk_id 世界，modify 中的 chunk
  上下文完整）。

  - 校验：`validate_prd_fsd_tdd_graph` + `validate_target_file_uniqueness`（归一化）+ **`validate_invariants`（六不变式：PRD↔1FSD、TDD↔1FSD/1制品、制品↔1TDD、关联经
  chunk、无孤儿、可追溯；另检树关系环——depends_on 环可诊断不拦）——供 version confirm 全局门禁消费**。'
