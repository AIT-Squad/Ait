id: chg-205
version: v2.13
type: MODIFY
target: tdd/[TDD]-index_manager#[TDD]-index_manager
author: system
date: '2026-06-27T11:19:14.364908+00:00'
message: 'v2.13: generative indexing domain'
base_hash: null
base_content: '<!-- @id:[TDD]-index_manager -->

  ## index_manager TDD


  ```yaml

  target_file: skill/ait/ait/index_manager.py

  ```


  ### 技术栈


  Python 3.10+ / pydantic / PyYAML / click（按需）。


  ### 文件职责


  IndexManager：baseline/version chunks-index 加载/保存/重建（rebuild_baseline）、query_baseline/query_version、summary 索引。


  ### 单元测试要求


  测试位于 tests/test_reindex.py：覆盖正常路径、边界与错误路径。'
new_content: '<!-- @id:[TDD]-index_manager -->

  ## index_manager TDD


  ```yaml

  target_file: skill/ait/ait/index_manager.py

  ```


  ### 技术栈

  Python 3.10+；依赖 `schemas`(BaselineIndex/BaselineChunkEntry/LinksIndex/VersionIndex/VersionChunkEntry/VersionIndexStats)、`chunk_parser`(parse_file/ParsedFile)、`yaml_io`(load_model/save_model)、`specgraph._global_category`(或等价)。


  ### 代码结构（IndexManager）

  - `IndexSchemaViolation(Exception)`。

  - 路径：`baseline_index_path/links_index_path/version_dir/version_index_path/find_baseline_file/find_version_file`。

  - 扫描重建：`scan_dir(base_dir)->list[ParsedFile]`(遍历 *.md parse_file)；`build_baseline()->BaselineIndex`(扫 docs/，每 chunk→BaselineChunkEntry，含
  file/heading/level/summary/metadata；global chunk 标 category)；`build_links()->LinksIndex`；`rebuild_baseline()->(BaselineIndex,LinksIndex)`(build
  + save 两者)。

  - 加载查询：`load_baseline/load_links`；`query_baseline(chunk_id)->BaselineChunkEntry|None`；`load_version_index(version)/save_version_index(idx)`；`query_version(version,chunk_id)`；`all_version_records(...)`；`list_versions()`。

  - 统计：`_compute_stats(idx)->VersionIndexStats`(total/by_action/by_state/tasks_summary)；`_compute_tasks_summary(version)`(读
  tasks/*.yaml 计 status)。


  ### 关键约定

  chunks-index 是「块状态台账」，与 specgraph「块关系」互补；baseline 从 docs/ markdown 重建（markdown 为真），version index 增量维护。


  ### 单元测试要求

  `tests/test_reindex.py`：rebuild_baseline 从 markdown 重建、query_baseline/version、stats 计数。pytest。'
