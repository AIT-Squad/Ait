id: chg-463
version: v2.38
type: MODIFY
target: fsd/[FSD]-ait-indexing#[FSD]-ait-indexing:TEST
author: system
date: '2026-07-14T13:56:04.631860+00:00'
message: new-model commit
base_hash: null
base_content: null
new_content: '<!-- @id:[FSD]-ait-indexing:TEST -->

  ## TEST 集成验收

  indexing 域所有部件(index_manager + migrations)合并到一起、作为整体的集成验收:

  1. WHEN 对含多个 chunk 文档的 docs/ 调 rebuild_baseline THEN 得到 BaselineIndex 与 LinksIndex,每个 chunk 按其 @id 与所在文件正确入索引,统计计数与实际 chunk
  一致。

  2. WHEN 用 chunk_id 调 query_baseline / 用 (version,chunk_id) 调 query_version THEN 命中返回对应条目、未命中返回 None;all_version_records
  返回该 chunk 在版本内的全部历史记录。

  3. WHEN save_version_index 写版本索引再 load_version_index 读回 THEN 得到等价索引(round-trip),三态/action/task 统计字段正确重算。

  4. WHEN 对含 block 术语的历史 .meta 调 migrate_block_to_chunk(dry_run=True) THEN 只返回预览报告不落盘;dry_run=False 则原子重命名键与文件并返回 MigrationReport。

  5. IF 迁移前置校验不通过 THEN 抛 MigrationError 且不做任何破坏性改动。'
