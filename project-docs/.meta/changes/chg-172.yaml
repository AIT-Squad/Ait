id: chg-172
version: v2.8
type: MODIFY
target: fsd/[FSD]-ait-foundation#[FSD]-ait-foundation:root
author: system
date: '2026-06-27T11:04:03.944366+00:00'
message: 'v2.8: generative foundation domain (root/io/yaml/hash TDD+FSD)'
base_hash: null
base_content: null
new_content: '<!-- @id:[FSD]-ait-foundation:root -->

  ## root

  ### 功能描述

  解析并校验 AIT 唯一合法工作根 `<CWD>/project-docs/`。提供 `resolve_project_root()` 与 `ProjectRoot(cwd/root/docs/meta)`。三类错误：CWD 在 project-docs
  内(E3 CWD_INSIDE_PROJECT_DOCS)、CWD 下无 project-docs(E1 NOT_AT_PROJECT_ROOT)、project-docs 缺 docs/.meta(E2 PROJECT_DOCS_MALFORMED)。**刻意不接受任何覆盖**（无
  --root/-C/AIT_ROOT、不向上递归、目录名硬编）。'
